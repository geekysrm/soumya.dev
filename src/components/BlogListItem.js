import Link from "next/link";

function BlogListItem(props) {
  console.log(props);
  return (
    <div>
      {/* Title: {title}
      Go read{" "}
      <Link href={`/${slug}`}>
        <a>here</a>
      </Link> */}
    </div>
  );
}

export default BlogListItem;
